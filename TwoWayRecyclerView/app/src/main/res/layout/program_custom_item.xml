<?xml version="1.0" encoding="utf-8"?>
<epg.duna.twowayrecyclerview.customviews.CustomLinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="@dimen/epg_item_container_height"
    android:background="@android:color/black"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingRight="1px"
    android:paddingBottom="1px">

    <TextView
        android:id="@+id/program_title"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.4"
        android:background="@color/default_color"
        android:gravity="center_vertical"
        android:singleLine="true"
        android:textColor="@android:color/white"
        android:textSize="13sp"
        app:drawableStartCompat="@drawable/ic_api_24px" />

    <TextView
        android:id="@+id/program_description"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.6"
        android:background="@color/default_color"
        android:gravity="center_vertical"
        android:singleLine="true"
        android:textColor="@android:color/white"
        android:textSize="18sp" />
</epg.duna.twowayrecyclerview.customviews.CustomLinearLayout>